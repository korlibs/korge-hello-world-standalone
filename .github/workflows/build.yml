name: "🧪 Testing"

on:
  push

concurrency:
  group: "testing"
  cancel-in-progress: false

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - { uses: actions/checkout@v4 }
      - { name: Build, run: ./korge build }
